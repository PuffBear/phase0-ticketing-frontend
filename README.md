# Phase0 Frontend

## Setup
1) Install deps:
```
npm install
```

2) Create env:
```
cp .env.example .env
```

3) Run:
```
npm run dev
```

## Env
- `VITE_API_BASE` -> backend API base URL (e.g. `http://localhost:3001`)
